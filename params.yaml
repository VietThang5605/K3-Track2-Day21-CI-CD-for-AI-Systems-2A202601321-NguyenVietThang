# Lựa chọn thuật toán: random_forest | extra_trees | hist_gradient_boosting | gradient_boosting | logistic_regression
model_type: "hist_gradient_boosting"

# Các siêu tham số
n_estimators: 300
max_depth: 15
min_samples_split: 5
learning_rate: 0.08